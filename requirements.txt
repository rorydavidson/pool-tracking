# Web framework & server
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
jinja2>=3.1
itsdangerous>=2.2

# Settings & validation
pydantic>=2.7
pydantic-settings>=2.3

# Persistence
SQLAlchemy>=2.0

# Security / crypto for credential storage
cryptography>=42.0

# Outbound HTTP for device integrations
httpx>=0.27

# Aiper HydroComm publishes water-quality readings only via its AWS IoT device
# shadow (MQTT-over-WebSocket); paho is the pure-Python client we connect with.
paho-mqtt>=2.1

# IANA timezone database for zoneinfo (the slim base image ships without it),
# used to display reading times in each pool's local timezone.
tzdata>=2024.1

# Claude API — generates pool chemistry advice on the fly
anthropic>=0.92

# Testing
pytest>=8.2
